# 🚀 Sistema de Detecção de Fraude com IA

Este projeto implementa um **modelo de Inteligência Artificial** simples para detecção de fraude em transações financeiras.  
Ele gera dados simulados, treina um modelo de machine learning e realiza previsões sobre novas transações.

---

---

## ⚙️ Funcionalidades

- **Gerar dados simulados** de transações (`gerar_csv.py`).
- **Pré-processar dados** (normalização e codificação de variáveis categóricas).
- **Treinar modelo de IA** usando regressão logística (`modelo.py`).
- **Avaliar desempenho** com métricas e matriz de confusão.
- **Salvar modelo treinado** em `fraud_model.joblib`.
- **Prever novas transações** com probabilidade de fraude (`predicao.py`).

---



